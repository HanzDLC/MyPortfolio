__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
.env
instance/
static/storage
.vercel

# Playwright screenshots & snapshots (auto-cleaned on idle)
*.png
*.jpeg
.playwright-mcp/

# Service-account / credential keys — never commit
openclaw123.json
*service*account*.json
*.iam.gserviceaccount.com.json

# Generated resume artifacts (rebuilt by generate_resume_docx.py on demand)
/Hanz_de_la_Cruz_Resume.docx
/Hanz_de_la_Cruz_Resume.pdf
.claude/.resume_publish_hash

# Private Drive publisher (personal folder ID + service-account path) — keep local
/publish_resume.py

# Client work product / secrets (NDA) — never commit
*backup codes*
/generate_response_doc.py
/Test Sales Automation.docx
/Test_Sales_Automation_Response_Hanz_de la Cruz.docx

# Remotion video source (kept local; not needed for the Flask deploy)
/remotion-landing/

# Large raw screen recordings (only the web-compressed versions are committed)
/static/Videos/aria-demo.mp4
/static/Videos/*Microsoft*.mp4
